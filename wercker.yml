box: wercker/ruby

build:
  steps:
    - bundle-install
    - script:
        name: spacebase build
        code: bundle exec rake spacebase:build
deploy:
    steps:
        # Execute the s3sync deploy step, a step provided by wercker
        - s3sync:
            key_id: $AWS_ACCESS_KEY_ID
            key_secret: $AWS_SECRET_ACCESS_KEY
            bucket_url: $AWS_BUCKET_URL
            source_dir: dist/
